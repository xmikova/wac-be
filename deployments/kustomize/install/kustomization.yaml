apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: pmdl-pharmacy-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: pmdl-pharmacy-webapi-config
    literals:
      - database=pmdl-pharmacy
      - collection=ambulance
patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: pmdl-pharmacy-webapi
