kind: Service
apiVersion: v1
metadata:
  name: pmdl-pharmacy-webapi
spec:
  selector:
    pod: pmdl-pharmacy-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
